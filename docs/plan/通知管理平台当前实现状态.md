# 通知管理平台当前实现状态

更新时间：2026-06-10

## 文档职责

本文是通知管理平台“当前已经实现什么、还缺什么、审计发现什么风险”的唯一事实源。状态必须以当前代码、数据库迁移和最近一次验证结果为准。

- 目标设计与历史决策：见 [notification-management-platform-feature.md](notification-management-platform-feature.md)
- 尚未闭环工作的实施计划：见 [notification-management-platform-closure-feature.md](notification-management-platform-closure-feature.md)

状态口径：

- `[x]`：代码已实现，并存在本地自动化验证或明确代码证据。
- `[~]`：主体代码已实现，但仍缺生产等价环境、真实外部平台验证或规模验证。
- `[ ]`：尚未实现。

本次审计口径：基于 2026-06-10 当前代码、`init.sql`、Flyway 迁移、前端 API 调用和测试文件进行核验；真实微信平台、真实 MySQL 压测和生产环境权限/日志不在本地静态审计中自动视为完成。

## 当前结论

通知管理平台的站内消息、Outbox、管理后台、系统公告、微信小程序订阅消息代码链路和 PC 运维主体已经实现。四期“通讯管理/小程序订阅运维”不能仅凭页面按钮和本地构建宣称 9.5+ 完整闭环，剩余门槛集中在：

1. 微信真实模板、真实授权、真实投递、正式公告触达和点击跳转仍缺联调证据。
2. 真实 MySQL 的索引命中、分页性能、Outbox 并发领取和大数据量验证仍缺记录。
3. 生产角色授权、按钮显隐、后端拒绝行为和历史敏感日志仍缺上线环境复核记录。
4. “跟进任务”尚未实现，当前只有用户级 `followUpRequired` 跟进标记。
5. 外部验证完成前，`WX-*`、`DB-*`、`OPS-*` 仍是四期闭环门槛；本地已补齐批量授权引导分页化、投递审计分页、模板维护校验和证据归档模板。
6. 小程序渠道仍存在模板无法真正删除/清空、微信消息未携带目标消息 ID、默认模板回退、永久错误误重试、授权次数并发一致性等代码缺口，需完成 `MP-*` 后再进行真实微信闭环验收。

## 已完成内容审计核验

| 能力 | 状态 | 核验结论 | 代码/迁移证据 | 剩余边界 |
| --- | --- | --- | --- | --- |
| 我的通知列表 N+1 优化 | `[x]` | 代码已改为先分页查询 `notification_recipient`，再按当前页 `messageId` 批量查询消息，避免用户列表页逐条查消息 | `NotificationService.getMyNotifications`、相关 Repository | 仍需真实 MySQL Explain 记录 |
| 微信小程序 Provider | `[~]` | `WechatMiniProgramNotificationProvider` 已调用微信 token 和订阅消息发送接口，接入 Outbox，能按返回码记录成功、失败或跳过 | `WechatMiniProgramNotificationProvider` | 微信消息跳转页尚未附加 `messageId`；永久错误分类和授权失效同步仍需完善；缺真实微信成功回执、错误码样本和跳转验证 |
| 小程序订阅授权链路 | `[~]` | 小程序端授权上报和后端授权次数保存链路已存在；订阅逻辑已抽为共享 composable，通知页和我的页复用同一套授权、状态展示和上报逻辑；Provider 投递前读取原始授权记录，不依赖快照误判 | `NotificationMiniProgramSubscriptionService`、`frontend-uniapp/src/composables/useMiniProgramSubscription.ts`、uni-app 通知页/我的页 | 授权次数扣减尚未使用数据库原子更新；模板较多时授权请求尚未分批；缺真机授权回归和微信后台模板匹配验证 |
| 小程序通知中心订阅入口 | `[x]` | 通知页已在消息摘要下方展示小程序订阅状态、模板授权状态、剩余次数和订阅按钮，符合“从通知管理进入订阅能力”的产品路径；我的页保留快捷入口但不复制授权逻辑 | `frontend-uniapp/src/pages/notifications/index.vue`、`frontend-uniapp/src/pages/profile/index.vue` | 真实微信授权弹窗只能在微信小程序真机/开发者工具内验证 |
| PC 渠道运行配置 | `[~]` | PC 可保存小程序渠道启用状态、AppID、AppSecret、模板映射、跳转页和超时；AppID 编辑时完整回写，AppSecret 加密保存且仅回显配置状态/来源/脱敏指纹，默认页和模板页由 `notification_mini_program_page` 字典约束 | `NotificationMiniProgramRuntimeConfigService`、`notification_channel_config`、`V40__notification_mini_program_page_dict.sql` | 删除模板仅移除前端行，后端合并逻辑仍保留旧模板；数据库清空后还可能回退环境默认模板；需生产配置加密密钥和权限复核 |
| 本地配置诊断 | `[x]` | 已检查 Provider 启用、AppID、AppSecret、模板字段、默认页和超时范围 | `testConfig()` | 只能证明本地配置完整，不能证明微信模板可用 |
| 远程 token 校验 | `[~]` | PC 可调用远程 token 测试接口，结果脱敏并记录操作日志 | `POST /api/admin/notifications/channels/{channel}/test-token` | 需使用生产等价 AppID/AppSecret 联调 |
| 模板校验 | `[~]` | 已实现模板 ID、字段映射和跳转页本地校验 | `NotificationMiniProgramRuntimeConfigService.diagnostics` | 需确认微信官方可用模板状态查询能力；若无接口，需保留人工审核记录和测试投递证据 |
| 测试投递 | `[~]` | 使用 `is_test=true` 隔离 delivery/outbox；创建归档 recipient，不进入用户站内列表；真实调用外部渠道，且跳过偏好延迟 | `NotificationService.createTestDelivery`、`NotificationOutboxService.processClaimed` | 会真实消耗用户订阅授权，需测试账号和操作提示/审计证据 |
| 订阅异常处理 | `[x]` | 支持已处理、暂不提醒、跟进标记、备注、重新打开；暂不提醒会阻止授权引导 | `notification_mini_program_resolution`、`AdminMiniProgramSubscriptionManagementService.resolve` | 无真实任务流转 |
| 跟进任务 | `[ ]` | 当前仅保存 `followUpRequired` 布尔标记，没有任务实体、负责人、截止时间、状态流转或任务列表 | `NotificationMiniProgramResolution.followUpRequired` | 需产品决策是否建设人工跟进任务子系统 |
| 订阅授权分页 | `[~]` | 已新增 `notification_mini_program_eligibility` 用户级资格快照；带聚合行状态筛选时通过 `sys_user` JOIN 快照表执行数据库分页，当前页再批量加载明细 | `V38__notification_mini_program_eligibility.sql`、`UserRepository.findActiveMiniProgramSubscriptionTargetsByEligibilityStatus` | 需真实 MySQL Explain 和 10 万级用户规模验证 |
| 细粒度权限 | `[~]` | 已拆分订阅查看、引导、异常处理、远程校验和测试投递权限，前后端均按权限控制 | `V36__notification_operations_hardening.sql`、`usePermission.ts`、Controller `@PreAuthorize` | 需生产角色矩阵和不同角色账号复核 |
| 敏感信息保护 | `[~]` | AppSecret 不回显；Provider 异常和 token 测试结果不返回 token；保存配置操作关闭参数日志；迁移清理历史敏感请求参数 | `NotificationMiniProgramRuntimeConfigService`、`WechatMiniProgramNotificationProvider`、`V36__notification_operations_hardening.sql` | 需生产历史日志、备份、导出文件巡检 |
| 批量授权引导规模优化 | `[x]` | 批量授权引导已改为按数据库分页批处理，避免一次性全量加载所有活跃用户；单角色场景可下推角色筛选，多角色场景分页后再按角色过滤 | `AdminMiniProgramSubscriptionManagementService.sendGuide` | 后续如需长期任务审计，可升级为异步批次任务 |
| 投递审计详情分页 | `[x]` | 已新增当前通知投递日志分页筛选接口，PC 投递审计按通知 ID、渠道、状态和关键词后端分页查询 | `GET /api/admin/notifications/{id}/delivery-logs`、`NotificationDeliveryLogRepository.findAdminDeliveryLogs`、`Notifications.vue` | 跨消息全局投递日志仍通过通知审计列表定位 |
| 渠道模板维护体验增强 | `[~]` | 小程序模板通知类型改为字典下拉；保存前校验重复类型、模板 ID 与字段映射配套；后端同步校验通知类型、重复模板和空字段映射 | `NotificationMiniProgramRuntimeConfigService.validateTemplateMapping`、`Notifications.vue` | 当前字段映射只读且使用预设展示；模板删除/清空语义尚未闭环；需增加可编辑映射和微信模板字段验证 |
| 按钮 icon 与样式统一 | `[x]` | 通知管理页主要命令按钮均已补齐 `btn-icon`，抽屉内关闭、引导、测试投递、保存处理结果等漏项已修复 | `Notifications.vue` | 项目未引入 lucide，当前不新增图标依赖 |
| 验证证据归档模板 | `[x]` | 已新增微信联调、MySQL Explain/并发、生产权限和敏感日志巡检证据模板 | `docs/archive/notification-management-verification-evidence-template.md` | 仍需真实执行后复制模板填写实际结果 |
| 当前自动化验证 | `[x]` | 2026-06-10 已重新执行后端 `mvn test`，66 项通过；PC 前端 `npm run build` 通过 | 后端 Surefire、PC 前端 Vite build | 构建存在 chunk 体积和插件耗时 warning；真实环境验证不属于本地自动化测试 |

## 未完成任务清单

| 编号 | 任务 | 当前状态 | 完成标准 |
| --- | --- | --- | --- |
| WX-01 | 真实微信 access_token 联调 | `[ ]` | 使用生产等价 AppID/AppSecret 执行远程 token 校验，记录时间、环境、操作者、脱敏结果和失败样本 |
| WX-02 | 真机订阅授权回归 | `[ ]` | 测试用户在微信真机完成 `requestSubscribeMessage`，后端保存模板授权状态和可用次数，PC 订阅授权页可查 |
| WX-03 | 隔离测试投递验证 | `[ ]` | PC 发起 `is_test=true` 测试投递，微信端收到消息；delivery ID、微信返回码、截图和日志全部脱敏归档 |
| WX-04 | 正式公告投递验证 | `[ ]` | PC `/notifications` 发布 `IN_APP + MINI_PROGRAM` 系统公告，站内通知可读，已授权用户收到微信订阅消息 |
| WX-05 | 小程序点击跳转验证 | `[ ]` | 小程序端已兼容 `messageId/id` 参数并可在列表加载后自动打开对应通知；仍需从微信订阅消息点击进入配置页面或业务页面，并能通过 REST 拉取对应通知/业务数据 |
| WX-06 | 模板状态验证策略 | `[ ]` | 先确认微信官方是否提供当前账号可用的模板状态查询接口；无接口时采用“后台审核截图 + 隔离测试投递”证据链 |
| MP-01 | 模板删除和清空语义修复 | `[ ]` | 前端删除模板或明确清空模板 ID 后，后端删除对应数据库模板配置；重新查询不再回显旧模板，且不会因环境默认模板自动恢复 |
| MP-02 | 微信消息精准跳转 | `[ ]` | Provider 发送到通知页时附加 `messageId` 参数并正确编码；小程序从微信消息进入后自动定位、打开并标记对应通知已读 |
| MP-03 | 默认配置与环境解耦 | `[ ]` | `application.yml` 不再预置具体 AppID 和正式模板 ID；新环境默认显示未配置，数据库明确清空后不会隐式回退到业务模板 |
| MP-04 | 微信错误码分类与授权失效同步 | `[ ]` | 将模板无效、字段错误、用户拒绝/授权失效等永久错误归类为不可重试；授权失效时同步修正本地状态和可用次数；临时网络错误仍可重试 |
| MP-05 | 模板字段映射编辑和验证 | `[ ]` | PC 支持编辑语义字段与微信字段编号映射，并提供格式校验、模板预览或测试投递验证；保存后可准确组装微信模板数据 |
| MP-06 | 授权次数并发一致性 | `[ ]` | 使用数据库条件更新、锁或预占机制保证同一次授权不会被并发投递重复使用，并补充并发自动化测试 |
| MP-07 | 小程序授权请求分批 | `[ ]` | 模板数量超过微信单次授权限制时可分批请求；界面分别显示每个模板的接受、拒绝、禁用和剩余次数状态 |
| DB-01 | 我的通知列表 Explain | `[ ]` | 在真实 MySQL 记录 `/api/notifications/my` 分页 recipient 和批量 message 查询 Explain |
| DB-02 | 未读数索引验证 | `[ ]` | 在真实 MySQL 记录 `/api/notifications/unread-count` 索引命中和响应耗时 |
| DB-03 | 订阅资格分页规模验证 | `[ ]` | 10 万用户规模下，带角色、关键词、行状态筛选的订阅授权分页命中索引，并记录 P50/P95/P99 |
| DB-04 | Outbox 并发验证 | `[ ]` | 多实例 worker 并发 claim 不重复领取；同业务聚合消息并发创建符合唯一约束预期 |
| OPS-01 | 生产权限矩阵复核 | `[ ]` | ADMIN、EDITOR、VIEWER 分别验证页签、按钮显隐和后端 API 403 行为，形成权限矩阵 |
| OPS-02 | 历史敏感日志巡检 | `[ ]` | 巡检 `operation_log`、投递日志、应用日志、代理日志、备份和导出文件，不出现 AppSecret、access_token、完整 OpenID |
| TASK-01 | 跟进任务产品决策 | `[ ]` | 明确是否只保留跟进标记；若要真实任务，另立任务实体、负责人、截止时间、状态流转和任务列表方案 |
| UX-01 | 渠道模板维护体验增强 | `[~]` | 已完成通知类型字典下拉、重复类型校验和基础字段映射配套校验；模板删除/清空与可编辑字段映射转由 MP-01、MP-05 继续闭环 |
| UX-02 | 批量授权引导规模优化 | `[x]` | 已改为数据库分页批处理，避免单次全量加载活跃用户 |
| UX-03 | 投递审计详情分页 | `[x]` | 已新增后端分页筛选接口，PC 当前通知投递日志不再只做前端过滤 |
| UX-04 | 小程序通知中心订阅入口 | `[x]` | 通知页已补充小程序订阅状态和授权按钮；我的页复用共享订阅逻辑作为快捷入口 |
| UI-01 | 按钮 icon 与样式统一 | `[x]` | 主要命令按钮已补齐 `btn-icon`，不新增外部图标依赖 |
| DOC-01 | 验证证据归档 | `[x]` | 已新增验证证据模板；真实联调/压测/生产巡检后需复制模板填写结果 |

## 审计发现的问题和隐患

| 编号 | 严重度 | 问题/隐患 | 影响 | 解决方案 |
| --- | --- | --- | --- | --- |
| R-001 | 高 | 真实微信联调证据缺失，代码已接入 Provider 但没有真实 token、模板、授权、投递、跳转闭环记录 | 无法证明小程序订阅消息在生产账号可用 | 执行 WX-01 至 WX-05，并将 evidence 归档 |
| R-002 | 高 | 测试投递会真实调用微信接口并消耗用户一次订阅授权 | 误用真实业务用户会降低授权余额，影响后续正式触达 | 只允许测试账号执行；PC 二次确认保留；后续可增加“测试用户白名单”或测试投递权限审批 |
| R-003 | 高 | 真实 MySQL Explain 和并发 Outbox 验证缺失 | 本地测试不能证明大数据量和多实例 worker 下性能与幂等可靠 | 执行 DB-01 至 DB-04，归档 SQL、数据规模和指标 |
| R-004 | 高 | 生产角色权限尚未复核 | 可能出现低权限账号能远程校验 token、测试投递或处理订阅异常 | 执行 OPS-01，用不同角色账号验证前端按钮和后端 `@PreAuthorize` |
| R-005 | 中高 | 历史敏感日志只通过迁移清理了部分 `operation_log.request_params`，没有覆盖备份、导出、代理日志和应用日志 | AppSecret、access_token、OpenID 仍可能存在历史介质中 | 执行 OPS-02，形成脱敏巡检记录 |
| R-006 | 中 | 批量授权引导已分页化，但仍是同步请求，未形成独立批次任务和发送明细表 | 大规模发送时请求耗时仍可能偏长，事后审计粒度有限 | 如业务需要批量发送审计，升级为异步批次任务并持久化明细 |
| R-007 | 中 | 订阅资格快照为最终一致，模板配置变化或事件遗漏时可能短暂显示旧状态 | PC 订阅授权列表可能短时间不准确 | 已有应用启动、配置变化和定时重建；后续增加“刷新中/配置指纹不一致”提示和手动重建入口 |
| R-008 | 中 | `V34` 先在订阅授权表增加异常处理列，`V35/V36` 又迁移到独立处理表并删除旧列 | 迁移顺序或半执行环境需要特别确认，避免上线执行失败 | 发布前在空库、旧库和预发库完整跑 Flyway；保留迁移顺序说明，不手工跳过 V34 |
| R-009 | 低 | 渠道模板维护已增加通知类型下拉和字段映射校验，但字段映射仍未提供完全图形化键值编辑 | 复杂模板字段调整仍需要约定字段预设或后续扩展 UI | 当前保留预设映射；需要更多模板时再扩展字段编辑器 |
| R-010 | 低 | 当前通知投递日志已后端分页，但跨消息全局 delivery 明细仍通过通知列表定位 | 运维人员按错误码跨消息检索时仍需先筛选通知 | 后续可新增全局 delivery 查询页或错误码聚合视图 |
| R-011 | 中 | `FOLLOW_UP` 只是跟进标记，不是任务系统 | 管理员可能误以为系统已生成可分派、可闭环的任务 | 文案统一为“跟进标记”；若业务需要任务，执行 TASK-01 |
| R-012 | 低 | 命令按钮已补齐 icon 容器，但项目未引入统一图标库 | 图标语义和视觉精度不如统一 icon 组件 | 后续若项目统一引入 lucide 或内部 Icon 组件，再替换字符图标 |
| R-013 | 高 | 渠道模板删除只移除前端编辑行，后端保存时从旧配置开始合并；空模板集合还会回退环境模板 | 管理员认为模板已删除，但运行时仍可能继续发送旧模板，配置状态与实际行为不一致 | 执行 MP-01，增加明确删除/清空协议和回归测试 |
| R-014 | 高 | 微信订阅消息跳转页未附加 `messageId`，小程序通知页虽支持目标消息参数，但发送端没有提供 | 用户点击微信消息只能进入页面，无法精准定位对应通知，WX-05 当前无法通过 | 执行 MP-02 后再进行 WX-05 真机验证 |
| R-015 | 中高 | `application.yml` 预置具体 AppID 和模板 ID，数据库缺失或清空时会回退环境默认模板 | 新环境可能被误判为部分配置，且管理员无法确认配置是否已真正清空 | 执行 MP-03，默认配置只保留空值和通用接口地址 |
| R-016 | 高 | 微信错误码除 `43101` 外大多按失败重试，用户拒绝后本地授权余额也不会同步清零 | 永久错误会反复重试；PC 可能继续显示已授权，造成运维误判和无效投递 | 执行 MP-04，建立错误码分类并同步授权状态 |
| R-017 | 中 | 模板字段映射在 PC 页面只读，后端只校验非空，无法证明字段编号与微信后台模板一致 | 模板调整后可能持续出现字段错误，必须依赖代码预设才能修改 | 执行 MP-05，并通过 WX-03/WX-06 验证真实模板 |
| R-018 | 高 | 授权次数采用“发送前查询、微信成功后扣减”的非原子流程 | 并发投递可能同时使用同一次授权，导致余额失真和部分消息失败 | 执行 MP-06，使用数据库原子扣减或授权预占 |
| R-019 | 中 | 小程序端一次将全部模板传入 `requestSubscribeMessage`，没有按微信单次限制分批 | 模板数量增长后授权请求可能失败或无法覆盖全部模板 | 执行 MP-07，增加分批授权和逐模板结果展示 |

## 状态纠偏记录

- N-003 的正确实现描述是“分页 recipient 后批量查询 message”，不是“join DTO 分页”。
- N-017、N-018 的代码实现已经存在，不再标记为未开始；真实微信联调作为独立验证项跟踪。
- “生成跟进任务”统一改称“设置跟进标记”；在真实任务模型落地前，不得宣称已生成任务。
- 小程序消息订阅入口以通知中心为主入口，我的页仅保留快捷入口；订阅授权逻辑统一收敛到 `useMiniProgramSubscription`。
- 远程 token 校验、隔离测试投递和异常处理已完成代码主体，不再在后续章节写作“待开发”；但真实微信证据仍未闭环。
- N-025/A-10 已通过资格快照和数据库 JOIN 分页完成代码闭环；快照只服务管理查询，真实投递继续读取原始授权记录。
- 四期当前结论应表述为“运维主体已实现，外部联调、规模验证和生产复核未闭环”，不得仅通过评分文字宣称 9.5+ 完成。
- `PC 渠道运行配置` 和 `渠道模板维护体验增强` 因模板删除/清空语义尚未闭环，状态从 `[x]` 纠偏为 `[~]`；完成 MP-01、MP-05 并通过回归验证后方可恢复为 `[x]`。
- WX-05 的真实点击跳转验证必须在 MP-02 补齐发送端 `messageId` 参数后执行；当前仅具备小程序接收参数的页面能力。
